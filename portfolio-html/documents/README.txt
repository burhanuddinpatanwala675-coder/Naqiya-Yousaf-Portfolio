Place the real CV Word document here, named exactly:

    teacher-cv.docx

The CV page (/src/pages/CV.jsx) links directly to /documents/teacher-cv.docx
for the "Download CV" button. If you use a different filename, update the
CV_FILE_PATH constant near the top of that file.

This site does NOT generate or fabricate a CV — this folder is
intentionally empty until the real document is supplied.

Place certificate files here (PDF, JPG, or PNG).

After adding a file, register it in /src/data/certificates.js so it
appears in the Certificate Gallery on the Credentials page. See that
file for the exact object structure and an example.

This folder is intentionally empty until real certificates are supplied.
